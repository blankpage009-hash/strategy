# 검색창 고도화 — 화면 디자인 (개념 5a)

> "What's the Strategy?" 회의자료 뷰어의 **상단 검색창**을 다시 디자인한다.
> 이 프로젝트에 이미 있는 **Dashboard Redesign(개념 2a) 화면과 Modernist 디자인 시스템을 그대로 이어받는다.** 새 화면이 아니라, 그 화면 위에서 검색이 어떻게 펼쳐지는지를 그리는 작업이다.

---

## 0. 맥락 — 지금 화면이 어떻게 생겼는가

- 단일 HTML 웹앱. 데스크톱(1440×900 기준)과 iPad에서 쓴다. 한국어 UI.
- 레이아웃(위→아래):
  1. **상단바 56px** — `[브랜드 268px | 🔍 검색 input (테두리 없이 밑줄 2px만) | Agenda · 설명서 · 설정 (56px 셀 ×3)]`
  2. **커맨드바 40px** — `FILTER` 라벨 + Select All/Deselect All + **회의 종류 칩**(다중 토글, 켜지면 빨간 채움) + ⊕ 추가 단추. 배경 `#eae9e9`, 아래 2px 검은 선.
  3. **3단** — 왼쪽 자료 목록 268px(연→월→자료 트리) / 가운데 PDF 뷰어(툴바 + 무대) / 오른쪽 NOTE 패널(요약·회의록·할 일).
- 회의 종류(태그) 목록: `손익보고 · 주간회의 · 임원회의 · 본부장회의 · 전략 워크샵 · 내부토의 · 비상경영회의 · 현안/이슈 · Report · 기타`
- 자료 하나의 메타데이터: 파일명(제목), 회의 종류, 날짜(연·월·주차, 예 `2026 · 08 · W3`), 쪽수, 회의록 유무.
- **지금의 검색**: 글자를 치면 화면 한가운데 **모달**(어두운 배경, 폭 640)로 결과가 뜬다. 결과는 자료 단위로 한 줄씩(제목 / 종류·날짜·쪽수 / 스니펫 1개 / "자료 12p에서 찾음"). 그룹 구분·연산자·최근 검색·키보드 이동이 없다. → **이것을 전부 바꾼다.**

## 1. 디자인 시스템 — 반드시 지킬 것

- Modernist 그대로: 글꼴 Archivo(한글은 시스템 고딕), 바탕 `#f3f2f2`, 보조 바탕 `#eae9e9`, 잉크 `#201e1d`, 보조 글자 `#605d5d` / `#7d7979`, 밝은 선 `#d7d3d3`, 굵은 선 `#201e1d` 2px, 강조 `#ec3013`, 강조 글자 `#ae1800`, 선택 배경 `#ffe0d9`, 하이라이트 `#ffc4b8`.
- **라운드 0. 그림자는 드롭다운 한 장에만**(`0 3px 10px rgba(45,43,43,.16)`). 새 색을 만들지 말 것.
- 아이콘은 Lucide. 이모지는 그룹 머리글의 작은 표식 정도로만.
- 단추 라벨은 왼쪽 정렬. 선으로 구획하고 여백으로 구획하지 않는다.
- 움직임은 색 전환 0.12s뿐. 드롭다운은 페이드/슬라이드 없이 바로 나타난다.

## 2. 바꾸는 방향 — 결정 사항

1. **모달 → 드롭다운.** 결과는 검색 input 바로 아래에 붙어서 떨어진다. 왼쪽 끝은 검색 셀의 왼쪽 선에, 폭은 검색 셀 폭(최소 560, 최대 760). 위쪽 선 2px 검정, 나머지 테두리 2px 검정, 그림자 1장. 뒤 화면은 어둡게 덮지 않는다(바깥 클릭·Esc로 닫힘).
2. **결과를 4개 그룹으로 나눈다** (있는 그룹만 표시, 순서 고정):
   - `📄 파일명 일치` — 제목에 검색어가 든 자료
   - `🏷 회의 태그` — 회의 종류 이름이 검색어와 맞는 것 (누르면 그 종류만 필터)
   - `📝 본문 내용 포함` — PDF 본문에서 걸린 자료. **키워드 앞뒤 스니펫 + 쪽 번호** 필수
   - `🗒 회의록 포함` — NOTE 패널의 회의록에서 걸린 자료
   각 그룹은 머리글(이름 + 건수)과 최대 5줄, 넘치면 `+ N건 더 보기`.
3. **Zero-state.** 검색창을 눌렀을 때(글자 없음) 같은 드롭다운에 `최근 검색어` / `최근 열람 문서` / 한 줄 문법 힌트를 보여준다.
4. **연산자를 입력칸 안에서 눈에 보이게** 만든다. 파싱된 조각은 칩·색으로 구분한다:
   - `#임원회의` → 태그 칩(빨간 테두리)으로 굳어짐
   - `title:임원회의` `cat:손익보고` `year:2026` `note:` → 필드 칩(검정 테두리, 필드명은 작게)
   - `-지입` 제외어 → 취소선 또는 회색 배경
   - `"연간 1.5억원 추가 수익"` 정확 일치 → 따옴표 포함 굵게
   - `모니터*` 와일드카드, `컴플라인언스~` 오타 허용 → 끝 기호만 강조색
   - `+` `/` 연결자 → 옅은 글자
5. **커맨드바 태그와 교집합.** 검색어가 있는 상태에서 커맨드바 칩을 누르면 드롭다운 머리글에 `필터: 손익보고 ✕` 가 붙고 결과가 좁혀진다.
6. **딥링크.** 본문 결과를 누르면 그 자료의 **해당 쪽**이 열리고 키워드가 하이라이트된다. 뷰어 툴바 아래 얇은 결과 이동 막대(`‘컴플라이언스’ 3 / 12  ◀ ▶  ✕`)로 다음/이전 결과를 오간다.
7. **단축키.** `Ctrl/⌘ K` 또는 `/` 로 검색창 포커스. 유휴 상태의 input 오른쪽 끝에 `⌘K` kbd 힌트를 옅게 둔다. 드롭다운 안에서 `↑ ↓` 이동, `Enter` 열기, `Tab` 다음 그룹, `Esc` 닫기.
8. **띄어쓰기 무시**는 백엔드 기능이라 화면에 드러내지 않는다. 다만 결과 스니펫에는 원문 그대로(`합동 모니터링`) 표시하고 하이라이트만 건다.

## 3. 그려야 할 프레임 (모두 1440×900, H만 예외)

같은 배경 화면(자료가 하나 열린 상태의 대시보드) 위에 검색 상태만 달리해서 그린다. 프레임 이름은 `5a-search-A` … `5a-search-H`.

### A. 유휴
- 검색창 포커스 전. placeholder `파일명 · 본문 · #태그 · title:` , 오른쪽 끝에 `⌘K` kbd(11px, 회색 테두리 1px).
- 나머지 화면은 2a 그대로.

### B. Zero-state (포커스, 글자 없음)
드롭다운 내용, 위→아래:
- `최근 검색어` 머리글(10px/800/.14em 대문자 스타일 그대로, 오른쪽에 `모두 지우기`) — 줄마다 🕘 아이콘 + 검색어 + 오른쪽 ✕. 최대 5줄. 예: `컴플라이언스` `유휴차량 -지입` `#임원회의 차량방치` `"연간 1.5억원 추가 수익"` `모니터*`
- `최근 열람 문서` 머리글 — 줄마다 제목 + 종류 · 날짜 · 쪽수. 최대 5줄.
- 바닥 줄(옅은 글자 11px, 위에 1px 실선): `+ 모두  /  하나라도  -제외  "정확"  끝*  오타~  #태그  title:` 와 오른쪽 끝 `문법 도움 ?`

### C. 결과 — 그룹화 드롭다운
검색어 `컴플라이언스`. 머리글 줄: `‘컴플라이언스’ · 14건` + 오른쪽 `✕`.
- `📄 파일명 일치 2` — 2줄
- `🏷 회의 태그 0` — 없으면 그룹 자체를 숨긴다(이 프레임에서는 숨김)
- `📝 본문 내용 포함 9` — 5줄 + `+ 4건 더 보기`. 각 줄:
  - 1행: 제목(12.5px/700) · 오른쪽 끝 `12p`(tabular)
  - 2행: `임원회의 · 2026 · 08 · W3 · 24p` (11.5px 회색)
  - 3행: 스니펫(12px, 1.5 행간) — `…내부 <mark>컴플라이언스</mark> 점검 결과 3건의 개선 과제가…` 하이라이트 `#ffc4b8`
  - 같은 자료에서 여러 쪽에 걸리면 3행 끝에 `+ 3곳`
- `🗒 회의록 포함 3` — 3줄, 스니펫 출처 표시 `회의록`
- 키보드 선택 줄: 배경 `#ffe0d9` + 왼쪽 3px 빨간 선. 마우스 hover: `#eae9e9`.
- 스크롤은 드롭다운 안에서. 그룹 머리글은 sticky.

### D. 연산자 표시 + 문법 도움
- 검색어 `#임원회의 "차량 방치" -지입 모니터*` 가 input 안에 조각별로 렌더된 모습(2-4 규칙대로). 오른쪽에 `?` 단추.
- `?` 를 누르면 드롭다운 오른쪽에 붙는 **문법 도움 패널**(폭 260, 왼쪽 2px 검정 선): 연산자 표(기호 / 뜻 / 예시) 8줄 — `+` `/` `-` `""` `*` `~` `#` `field:`. 예시는 실제 검색어 그대로: `임원회의 + 컴플라이언스`, `직영 / 위탁`, `유휴차량 -지입`, `"연간 1.5억원 추가 수익"`, `모니터*`, `컴플라인언스~`, `#임원회의 차량방치`, `title:임원회의`.
- 이 프레임은 결과 그룹도 함께 보여준다(태그 칩이 적용된 `📝 본문 내용 포함`만 남아 있는 상태).

### E. 커맨드바 태그와 교집합
- 검색어 `유휴차량` 입력 상태에서 커맨드바 `손익보고` 칩을 켠 직후.
- 드롭다운 머리글: `‘유휴차량’ · 6건` 옆에 필터 칩 `손익보고 ✕`. 커맨드바의 `손익보고` 칩은 켜진 상태(빨간 채움)로 강조.
- 결과는 손익보고 자료만.

### F. 결과 없음 + 오타 보정 제안
- 검색어 `컴플라인언스`. 드롭다운: `‘컴플라인언스’ 에 해당하는 자료가 없습니다.` 아래에
  - `혹시 → 컴플라이언스 (14건)` 링크 줄(빨간 글자)
  - `띄어쓰기·순서를 무시하고 찾으려면 컴플라인언스~ 로 검색` 안내 줄
  - 그 아래 `최근 검색어` 3줄

### G. 딥링크 — 뷰어에 열린 상태
- C 프레임의 본문 결과 하나를 누른 직후. 드롭다운은 닫힘, 검색창에는 검색어 유지.
- 가운데 뷰어: 12쪽이 열려 있고 본문 속 `컴플라이언스` 글자에 하이라이트(`#ffc4b8` 배경, 현재 항목은 `#ec3013` 2px 밑선).
- 뷰어 툴바 바로 아래 얇은 **결과 이동 막대**(높이 28, 배경 `#eae9e9`, 아래 1px 실선): `🔍 ‘컴플라이언스’  3 / 12` · `임원회의 · 2026 · 08 · W3 · 12p` · 오른쪽 `◀ ▶  ✕`. ◀▶는 **다른 자료의 결과로도** 넘어간다(자료가 바뀌면 왼쪽 목록 선택도 따라 바뀜).
- 왼쪽 자료 목록에서 해당 자료 줄이 선택 상태.

### H. 좁은 화면 — iPad 세로 820×1180, 폰 390×844 (두 장)
- 검색창 포커스 시 브랜드가 폴더 아이콘만 남고 검색 input이 상단바를 가득 채운다. `✕` 는 input 오른쪽.
- 드롭다운은 상단바 아래 **전체 폭 시트**(높이는 화면 끝까지, 안에서 스크롤). 그룹 머리글 sticky.
- 폰에서는 스니펫 2줄 말줄임, 2행 메타는 `임원회의 · 08 W3 · 12p` 로 축약.
- 결과 이동 막대(G)는 뷰어 위가 아니라 화면 **아래 고정 막대**로.

## 4. 예시 데이터 (그대로 써도 됨)

파일명 예시
- `2026년 8월 임원회의_컴플라이언스 점검 결과.pdf`
- `주간회의_유휴차량 운영 개선안(직영·위탁).pdf`
- `손익보고_8월 실적 및 하반기 전망.pdf`
- `본부장회의_합동 모니터링 체계 개편.pdf`
- `현안이슈_차량 방치 민원 대응.pdf`
- `전략 워크샵_2027 사업계획 방향.pdf`

스니펫 예시(원문식 띄어쓰기 유지)
- `…내부 <mark>컴플라이언스</mark> 점검 결과 3건의 개선 과제가 도출되었으며 9월 말까지…`
- `…<mark>유휴차량</mark> 27대 중 직영 전환 12대, 위탁 유지 15대로 조정 시 <mark>연간 1.5억원 추가 수익</mark>…`
- `…합동 <mark>모니터링</mark> 요원을 권역별로 배치하고 <mark>모니터</mark>링 주기를 월 1회로…`

## 5. 하지 말 것

- 둥근 모서리, 그라데이션, 반투명 유리 효과, 파란 포커스 링.
- 검색 결과를 카드로 띄우기 — 줄(row)과 실선으로만.
- 드롭다운 뒤를 어둡게 덮기.
- 그룹 아이콘을 크게·컬러로 만들기. 이모지는 12px 텍스트 크기로.
- 스니펫 없는 본문 결과. 본문 그룹은 반드시 스니펫 + 쪽 번호.

## 6. 산출물

- Claude Design 파일 1개, 프레임 A~H(H는 2장). 프레임마다 짧은 주석(어떤 상태인지).
- 마지막에 이전(2a·3a·4a)과 같은 형식의 **handoff 번들**: `5a-search-static.html`(A~G를 한 페이지에 세로로 나열, 정적) + `README.md`(드롭다운 치수·간격·글자 크기·색·상태별 규칙·키보드 동작 표).
