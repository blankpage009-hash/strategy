# 자료(PDF)와 데이터 분리 — 작업 계획

대상 파일: `whats-the-strategy.html` 한 개 (단일 파일 앱)

> 이 문서는 단계별로 **새 창(새 세션)** 에서 작업하기 위한 인수인계 문서입니다.
> 각 단계를 시작할 때 이 문서를 먼저 읽고, 해당 단계 절만 보고 작업하면 됩니다.
> 작업을 마치면 이 문서의 해당 단계에 `[완료]` 와 실제로 고친 내용을 적어 둡니다.

---

## 1. 배경 — 지금 무엇이 문제인가

PDF(자료)를 지우면 `removeDoc()` 이 그 자료에 딸린 데이터를 **전부 같이 지웁니다.**

```js
// whats-the-strategy.html 4628줄
delete KV.tasks[id];  delete KV.sums[id];
delete KV.scripts[id]; delete KV.memos[id];
// + IndexedDB: docs, files, draw, ainote, attach, attachfile
```

게다가 자료 id 는 업로드할 때마다 `uid()` 로 새로 만들어지므로(6472줄), 같은 PDF 를
다시 올려도 완전히 다른 자료가 되어 예전 데이터와 이어지지 않습니다.

### 이미 되어 있는 것 (중요)

이 앱은 **PDF 원본이 없는 자료 레코드**를 이미 지원합니다.
설정의 "글자와 요약만 저장(light)" 모드가 그것이고(6470줄), 저장 공간이 부족하면
자동으로 그 상태로 떨어집니다(6484줄). 즉 `docs` 레코드는 PDF 없이도 온전합니다.

**그래서 저장 구조를 바꿀 필요가 없습니다. 삭제 동작과 업로드 동작만 고치면 됩니다.**

---

## 2. 확정된 방침

> **"PDF 에 데이터가 붙어 있다" 가 아니라,
> "자료(회의) 레코드가 본체이고 PDF 는 거기 끼워 넣는 첨부물" 로 본다.**

- 데이터 열쇠는 지금처럼 **`doc.id` 를 그대로 씁니다.** (이관 작업 없음, 기존 백업과 100% 호환)
- 삭제를 두 갈래로 나눕니다 — ① **PDF 원본만 비우기** ② 자료 통째로 삭제
- 재업로드하면 같은 자료를 찾아 **새 id 를 만들지 않고 원본만 다시 채웁니다.**

### 검토했으나 채택하지 않은 방식

**자료이름 + 연/월/주차를 열쇠로 쓰는 방식** — 세 가지 이유로 제외했습니다.

1. Manage 창에서 이름·종류·주차는 글자칸 하나로 즉시 바뀝니다(3749줄).
   열쇠가 바뀔 때마다 6종 데이터를 전부 새 열쇠로 옮기는 코드가 필요하고,
   중간에 하나만 실패해도 데이터가 사라집니다.
2. 같은 주·같은 이름 자료 2개를 지금은 경고만 하고 허용합니다(6440줄).
   열쇠가 같아지면 두 자료가 회의록을 공유하게 됩니다.
3. 기존 데이터와 이미 내보낸 백업 파일(전부 id 기준)에 대해 되돌릴 수 없는
   일회성 대이관이 필요합니다.

---

## 3. 백업 대상 데이터 6종 — 실제 저장 위치

| 이름 | 저장 위치 | 열쇠 | PDF 삭제 시 지금 동작 |
|---|---|---|---|
| **Summary** | `KV.sums[id]` 의 `.a` `.ex` `.sug` | 자료 id | **지워짐** |
| **Note** (회의록) | `KV.sums[id].b` (직접 쓴 원문)<br>`ainote` 스토어 (AI NOTE)<br>`notes` 스토어 (옛 회의록) | 자료 id<br>자료 id<br>`회의종류\|날짜` | 지워짐<br>지워짐<br>**남음** |
| **Tasks** | `KV.tasks[id]` | 자료 id | **지워짐** |
| **Script** | `KV.scripts[id]` | 자료 id | **지워짐** |
| **Memo** | `KV.memos[id]` | 자료 id | **지워짐** |
| **회의 Agenda** | `KV.agendas[key]` | `회의종류\|날짜` | **남음** |

### 확인해 둘 두 가지

- **회의 Agenda 는 이미 PDF 와 무관합니다.** 열쇠가 `회의종류|날짜` 라서(3440줄)
  자료를 지워도 남습니다. 1·2단계에서 건드릴 것이 없고, 3단계 백업에는
  이미 포함되어 있습니다(6844줄). **추가 작업 없음.**
- **`notes` 스토어(열쇠 `회의종류|날짜`)와 `KV.sums[id].b` 가 둘 다 "회의록" 입니다.**
  화면 오른쪽 NOTE 탭은 `KV.sums[id].b` 를 씁니다. `notes` 스토어는 옛 방식으로
  보이며 `noteOf(d)` 로 읽기만 합니다. **1단계에서 이 둘의 관계를 먼저 확인하고,
  옛 것이면 이 문서에 사실을 적어 둘 것.** (지우지는 말 것)

### 첨부 파일(attach)에 대하여

AI Note 탭에 올린 첨부는 그 자체가 PDF 이므로 **"PDF" 쪽으로 분류합니다.**
목록(`attach` 스토어)과 원본(`attachfile` 스토어)이 나뉘어 있습니다.

---

## 4. 단계별 작업

작업 전에 설정에서 **"내보내기 (원본 포함)"** 으로 zip 백업을 한 번 받아 둘 것.

---

### [1단계] PDF 원본만 지우기 — Sonnet 5 · 노력 보통

**목표:** 자료 레코드와 데이터는 그대로 두고 PDF 원본만 비울 수 있게 한다.

1. `detachPdf(id)` 를 새로 만든다.
   - `dbDel("files", id)` 만 하고, `d.hasPdf = false` 로 바꿔 `dbPut("docs", stripDoc(d))`.
   - **`pageTexts` 는 반드시 유지한다** — 내용 검색이 계속 되어야 한다.
   - `draw`(펜 자국)는 지우지 않고 남긴다. 나중에 원본을 다시 붙이면 살아난다.
2. Manage 창의 ✕ 를 두 갈래로 바꾼다 (3773줄 근처).
   - "원본만 비우기" / "자료 통째로 지우기" / 취소
   - `confirm` 두 번은 헷갈리므로, 이미 있는 작은 쪽지 팝업 패턴(3540줄 `catMenu`)을
     따르거나 Manage 행에 단추를 두 개 두는 편이 낫다.
3. 선택 삭제(`#mgdelsel`, 3783줄)도 같은 두 갈래로.
4. 통째 삭제 확인창에 남아 있는 데이터 개수를 보여 준다 —
   "회의록 1 · 할 일 3개 · 메모 있음. 되돌릴 수 없습니다."
5. 원본이 없는 자료의 뷰어 빈 화면 문구:
   "원본이 없습니다 — 같은 이름으로 다시 올리면 이어붙습니다."
6. Manage 의 `mgMeta()`(3657줄)와 자료 목록의 "글자만" 표시를 그대로 재사용한다.

**주의:** `removeDoc()` 자체는 건드리지 말 것. 통째 삭제는 지금 동작이 맞다.

#### [완료] 1단계 (2026-09-10 · Sonnet 5)

**확인한 사실 — `notes` 스토어 vs `KV.sums[id].b`:**
- `notes` 스토어(→ `NOTES`, 열쇠 `cat|date`)는 **옛 방식**이다. 6196줄 주석에 명시:
  "화면 입력은 없어졌고, 옛 데이터와 백업 불러오기로만 채워집니다."
  코드상 `noteOf(d)` 로 **읽기만** 하고(AI 요약·검색·내보내기·목록 표시), 새로 쓰는 곳 없음.
- 화면 오른쪽 NOTE 탭이 쓰는 것은 `KV.sums[id].b` (4471줄, `sumB`). 이게 현재 본체.
- 결론: 둘은 별개다. `notes` 는 건드리지 않았고 지우지도 않았다. `detachPdf` 는 둘 다 유지.

**고친 내용 (`whats-the-strategy.html`):**
1. `detachPdf(id)` 신설 (removeDoc 바로 아래). `dbDel("files",id)` 만 하고
   `d.hasPdf=false` → `dbPut("docs", stripDoc(d))`. `pageTexts`·`draw`·모든 KV 데이터 유지.
   저장 실패 시 `loadAll()`+`renderAll()` 로 되돌림. 보고 있던 자료면 `loadPdfFor(d)` 로 갱신.
2. `docDataDesc(d)` 신설 — 통째 삭제 확인창용. "회의록 · 할 일 3개 · 요약 · 메모 · 첨부 PDF 2개 — 되돌릴 수 없습니다."
3. Manage 행: `hasPdf` 인 자료에 `원본 비우기` 단추 추가(`.mgdetach`, CSS 포함). ✕ 는 통째 삭제로 유지.
   `#mglist` click 핸들러를 `.mgdetach` / `.mgdel` 두 갈래로 재작성. ✕ 확인창은 `docDataDesc(d)` 사용.
4. 선택 삭제: footer 에 `선택 원본 비우기`(`#mgdetachsel`) 단추 추가. `mgSyncSel()` 이
   고른 자료 중 `hasPdf` 개수로 활성/개수 표시. 핸들러는 `detachPdf` 반복.
5. 원본 없는 자료 뷰어 문구: "원본이 없습니다 — … 같은 이름으로 다시 올리면 이어붙습니다."
6. `mgMeta()` 의 " · 글자만" 표시, 자료 목록 "글자만" 배지는 그대로 재사용(이미 있음).

**주의:** 브라우저가 `file://` 접근을 거부해 자동 검증은 못 했다. 수동으로 열어 Manage 창에서
원본 비우기 → 데이터 유지 → 재업로드 전까지 확인 필요. `removeDoc()` 은 건드리지 않았다.

---

### [2단계] 재업로드 이어붙이기 — Opus 5 · 노력 높음

**목표:** 같은 PDF 를 다시 올리면 예전 자료에 이어붙어 데이터가 그대로 돌아온다.

1. 이어붙일 대상 찾기 — 이미 있는 중복 감지 조건(6440줄)을 그대로 재활용한다.
   `d.title === p.title && d.cat === p.cat && d.date === date`
   여러 개면 **원본이 비어 있는(`hasPdf === false`) 것을 먼저** 고른다.
2. 업로드 목록(`renderPending`, 6332줄) 각 줄에 배지를 붙인다 —
   `↩ 이어붙임: <자료명>` + 해제 체크박스.
   등록하기 전에 새 자료로 올릴지 이어붙일지 눈으로 확인할 수 있어야 한다.
3. 이름을 바꿔서 못 찾는 경우를 위해 **수동 선택 드롭다운**을 둔다.
   (원본이 비어 있는 자료 목록에서 고르기)
4. `ingest()`(6437줄)에서 이어붙임 대상이 있으면 `uid()` 를 부르지 않고
   기존 doc 을 갱신한다 — `pages`, `pageTexts`, `size`, `hasPdf` 를 새 값으로,
   `addedAt` 은 유지, `indexDoc(d)` 로 검색 색인을 다시 만든다.
5. 쪽수가 달라졌으면 `draw`(펜 자국)를 어떻게 할지 한 번 물어본다.

**가장 조심할 곳:** `ingest()` 는 저장 실패 시 롤백 경로가 있다(6493줄).
이어붙이기가 중간에 실패해도 **기존 데이터가 절대 훼손되지 않도록** 할 것.
원본 저장이 성공한 뒤에 doc 을 갱신하는 순서를 지킨다.

#### [완료] 2단계 (2026-09-10 · Opus 5)

**고친 내용 (`whats-the-strategy.html`):**

1. `findRelink(p, taken)` 신설 — 중복 감지와 똑같은 조건
   (`d.title === p.title && d.cat === p.cat && d.date === date`).
   여러 개면 `!d.hasPdf` 인 것을 먼저 고른다. `mkDate()` 는 값이 비어도 문자열
   (`2026-NaN-01`)을 돌려주므로 **연·월·주차가 다 찼는지 먼저 본다.**
2. 업로드 목록에 `.frelink` 표시줄 추가 (`.frow` 를 `.fitem` 으로 감쌌다).
   - 초록 배지 `↩ 이어붙임: 이름 · 종류 · 날짜 주차` + 옆에 딸린 데이터 요약
     (`docDataDesc()` 재사용 — "회의록 · 할 일 3개 · 요약 · 메모")
   - 원본이 이미 있는 자료에 붙는 경우는 주황 배지 `(원본 교체)` 로 구분
   - `이어붙이기` 체크칸으로 해제 → `＋ 새 자료로 올립니다` + **수동 선택 드롭다운**
     (원본이 비어 있는 자료 전부에서 고른다)
   - 바닥글 `#bulkinfo` 에 `3개 준비됨 · 2개는 기존 자료에 이어붙입니다`
3. `syncRelink()` 가 목록을 다시 그릴 때마다 판정을 새로 한다.
   손으로 정한 줄(`p.rlSet`)은 그대로 두고 나머지만 자동으로 찾는다.
   **한 자료에 두 줄이 붙지 못하게** `taken` 으로 막고, 방금 고른 줄이 가져가도록
   `claimRelink()` 가 앞줄에서 떼어 낸다. 종류·날짜를 바꾸면(일괄 지정 포함)
   `rlSet` 을 풀어 다시 찾는다.
4. `ingest()` 는 `p.relinkId` 가 있으면 `uid()` 를 부르지 않고 `relinkDoc()` 로 넘긴다.
   `relinkDoc()` 은 **① 원본 저장 → ② doc 갱신** 순서를 지키고,
   ② 가 실패하면 `Object.assign(d, had)` + `indexDoc(d)` 로 화면을 되돌리고
   방금 넣은 원본을 지운다(전에 원본이 없던 경우만). `pages` · `pageTexts` ·
   `size` · `hasPdf` 만 새 값으로 바꾸고 **`id` · `title` · `cat` · `date` · `week` ·
   `addedAt` 은 기존 것을 그대로 둔다** (이름이 다르면 알려 준다).
   `indexDoc(d)` 로 색인을 다시 만들고 `SQC` 도 비운다.
   `storeMode === "light"` 인데 전에 원본이 있던 자료면 남은 원본을 치운다.
5. `trimDraw(d, 옛쪽수, 새쪽수)` — 쪽수가 **줄었고** 없어진 쪽에 실제로 펜 자국이
   있을 때만 한 번 물어본다. [확인] 그대로 두기 / [취소] 그 쪽들만 지우기.
   보고 있던 자료면 `drawLoadDoc(null)` 로 먼저 저장·분리한 뒤 손댄다.

**시험한 것** (localhost 로 띄워 확인 — 사용자 자료는 `file://` 출처라 무관):
- 자동 판정 · 배지 · 데이터 요약 · 수동 드롭다운 · 중복 차지 방지(양방향)
- 이어붙인 뒤 `id`·`addedAt` 유지, 요약·회의록·할 일·메모·스크립트 6종 전부 그대로
- **저장 실패 롤백** — 화면·저장소 모두 옛 값으로 복구, 새로 넣은 원본 삭제, 데이터 무사
- 펜 자국 [확인]/[취소] 두 갈래, 검색 색인 갱신(옛 낱말 안 걸림 / 새 낱말 걸림)
- `full`/`light` × `전에 원본 있음/없음` 네 가지 조합

**남겨 둔 것:** 이어붙인 자료는 `addedAt` 이 옛 날짜라, 등록 직후 폴더를 펼쳐 주는
동작(가장 최근 자료 기준)에서 눈에 안 띌 수 있다. 목록에는 정상으로 나온다.

---

### [3단계] 백업 3-옵션 — Sonnet 5 · 노력 낮음~보통

**목표:** `exportBackup(withPdf)` 를 `exportBackup(mode)` 로 바꾼다.

| 옵션 | 형식 | 담기는 것 |
|---|---|---|
| ① PDF 원본만 | .zip | `files/` + `attachfiles/` + `attach` 목록 + `docs` 메타 |
| ② 원본 + 데이터 전체 | .zip | 지금의 `exportBackup(true)` 와 동일 |
| ③ 데이터만 (원본 제외) | .json | 지금의 `exportBackup(false)` 와 동일 |

- **②③ 은 이미 되어 있다.** 6840줄 `exportBackup()` 을 보면 `kv` 에
  `sums · tasks · scripts · memos · agendas` 가 전부 들어 있고 `notes` · `ainote` 도 있다.
  6종 데이터가 모두 포함된다. **이름과 분기만 정리하면 된다.**
- **① 만 새로 만든다.** `backup.json` 에서 `kv` · `notes` · `ainote` 를 빼고
  `docs` 메타와 `attach` 목록만 남긴다.
- `docs` 메타는 **세 옵션 모두에 넣는다.** 어느 백업이든 단독으로 복원되어야 한다.
- 불러오기(6891줄)는 없는 항목을 건너뛰도록 이미 되어 있어 대부분 그대로 동작한다.
  확인 문구만 "PDF 원본만 있는 백업입니다" 처럼 구분해 준다.
- 설정창 단추(1785줄)를 3개로, 안내 문구(1781줄)도 함께 고친다.

#### [완료] 3단계 (2026-09-10 · Sonnet 5)

**고친 내용 (`whats-the-strategy.html`):**
1. `exportBackup(withPdf)` → `exportBackup(mode)`. `mode` 는 `"data"` / `"full"` / `"pdf"`.
   옛 호출 호환으로 `true→"full"`, `false→"data"` 도 받는다.
   - `meta.backupKind` 에 mode 를 적어 둔다(불러오기에서 구분용).
   - `docs`(자료 목록)는 **세 옵션 모두**에 넣는다.
   - `"pdf"` 는 `kv`·`notes`·`ainote` 를 빼고 `docs` + `attach` 목록 + `files/`·`attachfiles/` 원본만 담는다.
   - `"full"` 은 기존 `exportBackup(true)` 와 동일. `"data"` 는 기존 `exportBackup(false)` 와 동일.
   - zip 파일명: `백업-원본포함-` / `백업-PDF원본만-`, 완료 토스트도 구분.
2. 설정창 단추 3개: `데이터만 (.json)` `원본 + 데이터 (.zip)`(`#btn-export-full` 신설) `PDF 원본만 (.zip)`. 안내 문구 교체.
3. 불러오기 확인창: `data.backupKind === "pdf"`(또는 zip인데 kv·notes 없음)면
   "PDF 원본만 있는 백업입니다 … 데이터는 건드리지 않습니다" 로 구분. 불러오기 로직 자체는
   없는 항목을 건너뛰므로 그대로 동작(수정 없음).

**주의:** `file://` 접근 거부로 자동 검증 못 함. 수동으로 세 단추 각각 내보내기 → 불러오기 확인 필요.

---

### [4단계] 마무리 — Sonnet 5 · 노력 낮음

1. 자료 목록·Manage 의 "원본 없음" 표시를 통일한다.
2. (선택) "원본 없는 자료만 보기" 필터.
3. **버그 하나 같이 고치기** — "전부 지우기"(6972줄)가 `attach` · `attachfile`
   스토어를 지우지 않는다. 전부 지웠는데 첨부 PDF 가 용량을 계속 차지한다.

#### [완료] 4단계 (2026-09-10 · Sonnet 5)

**고친 내용 (`whats-the-strategy.html`):**
1. "원본 없음" 표시는 이미 일관됨 — 자료 목록 배지 `글자만`, `mgMeta()` ` · 글자만`,
   뷰어 빈 화면 `원본이 없습니다 … 같은 이름으로 다시 올리면 이어붙습니다`. 문구 통일 확인, 수정 불필요.
2. Manage 창에 `원본 없는 자료만` 체크칸(`#mgnopdf`) 추가. `mgRows()` 에서 `!d.hasPdf` 로 거른다.
   `openManage()` 에서 초기화, `change` → `mgRender()`.
3. **버그 수정** — `#btn-wipe`(전부 지우기)에 `dbClear("attach")` · `dbClear("attachfile")` 추가,
   메모리 `ATTACH = []` 도 초기화. 이제 첨부 PDF 도 함께 지워진다.

---

## 5. 작업 기록

- [x] 1단계 — PDF 원본만 지우기(`detachPdf`), Manage 창 두 갈래(행 단추 + 선택 단추), 통째 삭제 확인창에 데이터 개수 표시. `notes`=옛방식 확인 완료.
- [x] 2단계 — 재업로드 이어붙이기(`findRelink`·`relinkDoc`), 업로드 목록 배지 +
  해제 체크칸 + 수동 드롭다운, 쪽수 줄었을 때 펜 자국 처리(`trimDraw`).
  `id`·`addedAt` 유지 · 저장 실패 롤백 확인.
- [x] 3단계 — `exportBackup(mode)` 3-옵션(`data`/`full`/`pdf`), 설정창 단추 3개 + 안내 문구,
  불러오기 확인창에 "PDF 원본만" 구분. `docs` 는 세 옵션 모두 포함.
- [x] 4단계 — "원본 없음" 표시 일관성 확인(수정 불필요), Manage `원본 없는 자료만` 필터(`#mgnopdf`),
  "전부 지우기" 가 `attach`·`attachfile` 도 지우도록 버그 수정.
